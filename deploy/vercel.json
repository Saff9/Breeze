{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "echo 'No build step needed — Breeze runs on the Node runtime'",
  "installCommand": "npm install",
  "framework": null,
  "routes": [
    { "src": "/(.*)", "dest": "/api/index" }
  ]
}
